import React from 'react';
import ReactDOM from 'react-dom/client';
import EchoChamberSimulator from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EchoChamberSimulator />
  </React.StrictMode>
);